// Initialize Alpine.js building state component
import { createBuildingState } from './state.ts';

// Re-export createBuildingState for use in components
export { createBuildingState };

// Note: buildingStateData registration has been moved to individual components
// to ensure proper scoping and avoid conflicts
