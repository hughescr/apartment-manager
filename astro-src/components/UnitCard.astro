---
// astro-src/components/UnitCard.astro
// DEPRECATED: This is a compatibility wrapper. Use UnitCardProvider instead.
import { UnitCardProvider } from './unit-card';
import type { UnitData, UnitTypeData } from '../types';

const { unit, unitTypes = [] } = Astro.props as { unit: UnitData, unitTypes: UnitTypeData[] };
---

<!-- Backward compatibility wrapper - delegates to the new modular UnitCardProvider -->
<UnitCardProvider unit={unit} unitTypes={unitTypes} />
