---
// astro-src/components/UnitTypeForm.astro
// Now uses the modular UnitTypeFormProvider pattern
import { UnitTypeFormProvider } from './unit-type-form';

const { buildingID, onSuccess } = Astro.props as {
    buildingID: string
    onSuccess?: string // JavaScript code to execute on success
};
---

<UnitTypeFormProvider buildingID={buildingID} onSuccess={onSuccess} />
